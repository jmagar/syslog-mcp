server {
    listen 443 ssl;
    server_name syslog.*;

    include /config/nginx/ssl.conf;

    location / {
        include /config/nginx/proxy.conf;
        proxy_pass http://syslog-mcp:8080;
    }
}
