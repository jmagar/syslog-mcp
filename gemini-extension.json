{
  "name": "syslog-mcp",
  "version": "0.8.0",
  "description": "Homelab syslog receiver and MCP server for searching, tailing, and correlating logs across hosts.",
  "mcpServers": {
    "syslog-mcp": {
      "type": "http",
      "url": "http://localhost:3100/mcp",
      "headers": {
        "Authorization": "Bearer ${SYSLOG_MCP_TOKEN}"
      }
    }
  },
  "settings": [
    {
      "name": "Database Path",
      "description": "Path to the SQLite database for syslog storage (e.g., /data/syslog.db)",
      "envVar": "SYSLOG_MCP_DB_PATH",
      "sensitive": false
    },
    {
      "name": "Syslog Listen Port",
      "description": "UDP/TCP port for receiving syslog messages (default: 1514)",
      "envVar": "SYSLOG_PORT",
      "sensitive": false
    },
    {
      "name": "MCP Bearer Token",
      "description": "Optional bearer token for authenticating requests to the MCP HTTP endpoint",
      "envVar": "SYSLOG_MCP_TOKEN",
      "sensitive": true
    }
  ]
}
