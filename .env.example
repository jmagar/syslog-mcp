# API Keys (Required to enable respective provider)
ANTHROPIC_API_KEY="your_anthropic_api_key_here"       # Required: Format: sk-ant-api03-...
PERPLEXITY_API_KEY="your_perplexity_api_key_here"     # Optional: Format: pplx-...
OPENAI_API_KEY="your_openai_api_key_here"             # Optional, for OpenAI models. Format: sk-proj-...
GOOGLE_API_KEY="your_google_api_key_here"             # Optional, for Google Gemini models.
MISTRAL_API_KEY="your_mistral_key_here"               # Optional, for Mistral AI models.
XAI_API_KEY="YOUR_XAI_KEY_HERE"                       # Optional, for xAI AI models.
GROQ_API_KEY="YOUR_GROQ_KEY_HERE"                     # Optional, for Groq models.
OPENROUTER_API_KEY="YOUR_OPENROUTER_KEY_HERE"         # Optional, for OpenRouter models.
AZURE_OPENAI_API_KEY="your_azure_key_here"            # Optional, for Azure OpenAI models (requires endpoint in .taskmaster/config.json).
OLLAMA_API_KEY="your_ollama_api_key_here"             # Optional: For remote Ollama servers that require authentication.
GITHUB_API_KEY="your_github_api_key_here"             # Optional: For GitHub import/export features. Format: ghp_... or github_pat_...

# ==========================================
# Syslog MCP Server Configuration
# ==========================================

# Elasticsearch Configuration
# The URL of your Elasticsearch cluster
ELASTICSEARCH_HOST=squirts:9200

# Optional: Elasticsearch authentication
# Leave empty if no authentication is required
ELASTICSEARCH_API_KEY=
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=

# Optional: SSL/TLS configuration
ELASTICSEARCH_VERIFY_CERTS=false
ELASTICSEARCH_USE_SSL=false

# Optional: Connection settings
ELASTICSEARCH_REQUEST_TIMEOUT=30
ELASTICSEARCH_MAX_RETRIES=3

# MCP Server Configuration
# Optional: Bearer token for MCP server authentication
AUTH_TOKEN=

# Server settings (used by run.sh script)
MCP_HOST=localhost
MCP_PORT=8005
MCP_TRANSPORT=http
MCP_LOG_FILE=logs/syslog_mcp_server.log

# Legacy server settings (kept for compatibility)
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Logging Configuration
# Available levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Log file path (leave empty for console only)
LOG_FILE=

# Optional: Enable structured JSON logging (true/false)
LOG_JSON=true

# Development settings
DEBUG=false

# Optional: Cache settings
CACHE_MAX_SIZE=1000
CACHE_TTL=300

# Optional: Export directory for syslog_export tool
# Directory where exported log files will be saved
# Default: /tmp/syslog-exports if not set
SYSLOG_EXPORTS=/tmp/syslog-exports

# ==========================================
# Gotify Configuration (Alert Notifications)
# ==========================================

# Gotify server URL (for sending alert notifications)
GOTIFY_URL=https://gotify-server:443

# Gotify application token (create an app in Gotify admin)
# Format: Axxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GOTIFY_TOKEN=your_gotify_app_token_here
