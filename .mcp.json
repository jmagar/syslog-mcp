{
  "mcpServers": {
    "syslog-mcp": {
      "type": "http",
      "url": "${user_config.syslog_mcp_url}",
      "headers": {
        "Authorization": "Bearer ${user_config.syslog_mcp_token}"
      }
    }
  }
}
